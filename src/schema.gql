# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: ID!
  email: String!
  name: String
  posts: [Post!]
}

type Post {
  id: ID!
  title: String!
  content: String
  published: Boolean
  author: User
}

type Query {
  """It returns all registered users"""
  users: [User!]!

  """get one user by id"""
  user(id: Float!): User!

  """It returns all post"""
  posts: [Post!]!

  """get post by Id"""
  post(id: Float!): Post

  """It returns all post where published is true"""
  feed: [Post!]
}

type Mutation {
  """It deletes a user with specific ID"""
  deleteUser(id: Float!): User

  """Create a draft for a user"""
  createDraft(draftData: DraftDataInput!): Post

  """Enable the published post property to true"""
  publish(id: Float!): Post

  """It deletes a post with specific ID"""
  deletePostById(id: Float!): Post
}

"""New Draft Input"""
input DraftDataInput {
  title: String!
  content: String!
  authorEmail: String!
}
